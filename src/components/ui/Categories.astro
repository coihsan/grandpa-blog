---
import { getCollection } from "astro:content";
const posts = await getCollection('posts')
const category = [...new Set(posts.map((post) => post.data.category))]
---
<ul class="grid">
    {category.map((index) => {
        <li>{index}</li>
    })}
</ul>
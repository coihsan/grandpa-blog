---
const {className, customPadding} = Astro.props
---
<section class={`${customPadding} relative w-full mx-auto py-8 lg:py-12 xl:py-16 ${className}`}>
    <main class="max-w-screen-xl mx-auto w-full h-full px-5 md:px-0 lg:px-0">
        <slot/>
    </main>
</section>